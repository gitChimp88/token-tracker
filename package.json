{
  "name": "token-tracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ gitchimp88/token-tracker",
    "create-local": "graph create --node http://localhost:8020/ gitchimp88/token-tracker",
    "remove-local": "graph remove --node http://localhost:8020/ gitchimp88/token-tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gitchimp88/token-tracker",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
